version: '3'

preconditions:
  - sh: "[ 1 = 0 ]"
    msg: "own file precondition failed"

tasks:
  skip-task:
    skip_preconditions: true
    cmds:
      - echo "should not run - own file precondition still applies"
