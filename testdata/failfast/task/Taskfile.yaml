version: '3'

tasks:
  default:
    deps:
      - dep1
      - dep2
      - dep3
      - dep4
    failfast: true

  dep1: sleep 5 && echo 'dep1'
  dep2: sleep 6 && echo 'dep2'
  dep3: sleep 7 && echo 'dep3'
  dep4: exit 1
