version: '3'

vars:
  contents2:
    sh: "cat b"
  pwd2:
    sh: "pwd"

tasks:
  run:
    vars:
      contents2_local:
        sh: "cat b"
    cmds:
      - cmd: 'echo "contents: {{ .contents2 }} {{ .contents2_local }} {{ .pwd2 }}"'
