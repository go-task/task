version: '3'

vars:
  contents1:
    sh: "cat a"
  pwd1:
    sh: "pwd"

tasks:
  run:
    vars:
      contents1_local:
        sh: "cat a"
    cmds:
      - cmd: 'echo "contents: {{ .contents1 }} {{ .contents1_local }} {{ .pwd1 }}"'
