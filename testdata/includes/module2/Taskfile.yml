version: '3'

vars:
  DIR:
    sh: basename "$(pwd)"

tasks:
  gen_dir:
    cmds:
      - 'echo "{{.DIR}}: included_directory_with_dir"'

  gen_file:
    cmds:
      - 'echo "{{.DIR}}: included_taskfile_with_dir"'
