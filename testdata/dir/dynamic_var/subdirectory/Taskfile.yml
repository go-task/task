version: '3'

vars:
  TASKFILE_DIR:
    sh: basename "$(pwd)"

tasks:
  from-included-taskfile:
    cmds:
      - echo '{{.TASKFILE_DIR}}'

  from-included-taskfile-task:
    cmds:
      - echo '{{.TASKFILE_TASK_DIR}}'
    vars:
      TASKFILE_TASK_DIR:
        sh: basename "$(pwd)"
