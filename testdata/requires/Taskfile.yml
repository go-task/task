version: '3'

tasks:
  default:
    - task: missing-var

  missing-var:
    requires:
      vars:
        - foo
    cmd: echo "{{.foo}}"


  validation-var:
    requires:
      vars:
        - name: foo
          allowed_values: ['one', 'two']
