version: '3'

includes:
  remote-2:
    taskfile: "{{ .HTTP_URL }}/Taskfile3.yml"
    dir: ./dir-2
  remote-1:
    taskfile: "{{ .HTTP_URL }}/Taskfile3.yml"
    dir: ./dir-1

tasks:
  default: "true"
