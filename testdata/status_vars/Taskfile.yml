version: '3'

tasks:
  build-with-checksum:
    sources:
      - ./source.txt
    method: checksum
    status:
      - echo "{{.CHECKSUM}}"

  build-with-timestamp:
    sources:
      - ./source.txt
    method: timestamp
    status:
      - echo '{{.TIMESTAMP.Unix}}'
      - echo '{{.TIMESTAMP}}'
