version: '3'

includes:
  second: ./Taskfile.yml

tasks:
  write-file:
    requires:
      vars: [CONTENT, OUTPUT_FILE]
    cmd: |
      echo "{{ .CONTENT }}" > "{{ .OUTPUT_FILE }}"

  second-taskfile2-task:
    cmd: echo {{ .TASK }}
